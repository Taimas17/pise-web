# Monorepo PISE — Bootstrap pilote (backend + web)

Résumé
- Ajout du monorepo avec `/backend` (Laravel 11, Sanctum, exports PDF/Excel, géoloc POINT SRID 4326, chiffrage AES PII) et `/web` (React TS PWA, offline de base, carte Leaflet)
- Implémentation des modèles/migrations et endpoints principaux (auth, reports CRUD + workflow review/assign, taxonomies, stats, exports)
- Docs initiales (`docs/openapi.yaml`, ADR, modèle, plan de sauvegardes). Stubs Kobo (service + job + commande).

Impact
- Permet de démarrer les essais du pilote en local avec variables `.env.example`.
- Structure extensible pour intégration Kobo et client Android (OpenAPI fourni).

Notes
- Les clés de chiffrement colonne ne sont pas versionnées (`COLUMN_ENCRYPTION_KEY`).
- Les coordonnées précises sont chiffrées; la colonne `location` contient une version masquée si non-public.


₍ᐢ•(ܫ)•ᐢ₎ Generated by [Capy](https://capy.ai) ([view task](https://capy.ai/project/572ac116-84af-11f0-a94e-3eef481a796b/task/73d380e2-d21d-4682-932d-7acdcbe34e78))